{
  "name": "marvelHeroProject",
  "version": "1.0.0",
  "description": "Application mobile first all Marvel api build with node and Vue",
  "main": "server.js",
  "scripts": {
    "test": "cross-env NODE_ENV=test jest --testTimeout=5000",
    "start": "node src/server.js"
  },
  "author": "Tess",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.24.0",
    "cross-env": "^7.0.3",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "md5": "^2.3.0",
    "supertest": "^6.3.3"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
